#!/usr/bin/env bash
curl -s https://www.scala-lang.org/download/all.html | \
    grep 'download/' | grep 'Scala' | grep -v 'development' | \
    awk -F'"' '{print $2}' | awk -F'/' '{print $3}' | \
    sed -e 's/.html//g' | sed -e 's/\n/ /g' | tr '\n' ' '
echo ""