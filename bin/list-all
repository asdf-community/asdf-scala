#!/usr/bin/env bash

get_scala_versions() {
curl -s https://www.scala-lang.org/download/all.html \
  | grep '/download.*Scala' \
  | grep -v 'develop' \
  | sed -e 's|.*download/\(.*\)\.html.*|\1|'
}

get_scala_versions | sort | tr "\n" " "
